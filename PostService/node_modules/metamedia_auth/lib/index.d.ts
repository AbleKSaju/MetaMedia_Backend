import { Request, Response, NextFunction } from 'express';
import { JwtPayload } from 'jsonwebtoken';
declare global {
    namespace Express {
        interface Request {
            user?: JwtPayload;
        }
    }
}
declare function verifyToken(req: Request, res: Response, next: NextFunction): void;
export default verifyToken;
